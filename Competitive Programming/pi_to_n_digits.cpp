#include <iostream>
#include <iomanip>
#define _USE_MATH_DEFINES
#include <cmath>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    int N = 5;
    // cin >> N;
    for (int i=0; i<=N; i++){
        cout << setprecision(i+1) << M_PI << "\n";
    }
    return 0;
}