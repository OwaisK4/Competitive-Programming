#include <iostream>
#include <iomanip>
#include <vector>
#include <algorithm>
#include <utility>
#include <map>
#include <queue>
#include <climits>
#define rep(i,a,n) for (int i=a; i<n; i++)
#define all(a) a.begin(), a.end()
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--){
        long long int n, x;
        cin >> n >> x;
        vector<int> a(n);
        rep(i, 0, n){
            cin >> a[i];
        }
    }

    return 0;
}